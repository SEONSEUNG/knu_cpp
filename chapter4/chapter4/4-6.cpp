#include <iostream>
#include <string>
using namespace std;

/*
int main() {

	while (1) {

		string str;
		int len;

		cout << "아래에 한 줄을 입력하세요.(exit를 입력하면 종료합니다)" << endl << ">>";
		getline(cin, str);

		if (str == "exit") break;

		len = str.size();

		for (int i = len; i >= 0; i--) {
			cout << str[i];
		}
		cout << endl;

	}

}
*/