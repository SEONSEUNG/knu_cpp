#include <iostream>
#include <cstring>
#include <string>
using namespace std;

/*
int main() {

	string longname;
	string name;
	int len = 0;

	cout << "5 명의 이름을 ';'으로 구분하여 입력하세요\n>>";

	for (int i = 1; i < 6; i++) {

		char input_name[100];
		cin.getline(input_name, 100, ';');
		name = input_name;

		cout << i << " : " << name << '\n';

		if (strlen(input_name) >= len) {
			longname = name;
			len = strlen(input_name);

		}

	}

	cout << "가장 긴 이름은 " << longname;

}
*/