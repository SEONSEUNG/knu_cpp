#include <iostream>

using namespace std;

#include "3-10_header.h"

/*
int main() {

	Add a;
	Sub s;
	Mul m;
	Div d;
	int n1, n2;
	char c;

	while (1) {
		cout << "두 정수와 연산자를 입력하세요>>";
		cin >> n1 >> n2 >> c;
		switch (c) {
		case '+':
			a.setValue(n1, n2);
			cout << a.calculate() << endl;
			break;
		case '-':
			s.setValue(n1, n2);
			cout << s.calculate() << endl;
			break;
		case '*':
			m.setValue(n1, n2);
			cout << m.calculate() << endl;
			break;
		case '/':
			d.setValue(n1, n2);
			cout << d.calculate() << endl;
			break;
		}

	}

}
*/