#include <iostream>
#include <cstring>
#include <string>
using namespace std;
/*
class Account {
public:
	Account(string str, int new_id, int bal);
	int deposit(int amount);
	int withdraw(int amount);
	string getOwner();
	int inquiry();
private:
	string name;
	int id;
	int balance;
};

Account::Account(string str, int new_id, int bal) {
	name = str;
	balance = bal;
}

int Account::deposit(int amount) {
	balance += amount;
	return balance;
}

int Account::withdraw(int amount) {
	balance -= amount;
	return balance;
}

string Account::getOwner() {
	return name;
}

int Account::inquiry() {
	return balance;
}

int main() {
	Account a("kitae", 1, 5000);
	a.deposit(50000);
	cout << a.getOwner() << "ÀÇ ÀÜ¾×Àº " << a.inquiry() << endl;
	int money = a.withdraw(20000);
	cout << a.getOwner() << "ÀÇ ÀÜ¾×Àº " << a.inquiry() << endl;
}
*/